#include <openssl/rand.h>   // For random number generation (e.g., AES key generation)
#include <openssl/aes.h>    // For AES encryption
#include <openssl/evp.h>    // For EVP functions (optional, depending on your needs)
#include <openssl/sha.h>    // For SHA functions (optional, depending on your needs)
#include <stdio.h>
#include <stdlib.h>
